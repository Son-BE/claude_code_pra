{
  "name": "claude-code-pra",
  "version": "1.0.0",
  "private": true,
  "description": "매출 대시보드 + 소셜 로그인(Google, 카카오, 네이버)",
  "main": "server/index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch --env-file-if-exists=.env server/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^5.2.1",
    "express-session": "^1.19.0"
  }
}
